// 堆排序
// 堆结构：堆是一个完全二叉树
// 完全二叉树：从左往右依次补齐
// 节点i的左子节点为 2 * i + 1，右子节点为 2 * i - 1，父节点为 (i - 1) / 2
// 大根堆：任何一个子树的最大值都是子树都头部，小根堆：任何一个子树的最小值都是子树都头部
function stackSort(arr) {
  for(let i = 0; i < arr.length;i++) {

  }
}

function shangfu(arr) {
   
}